FROM node:20-alpine

# Set working directory
WORKDIR /app

# Copy package files
COPY package.json package-lock.json ./

# Install dependencies
RUN npm ci

# Copy all files (this will be overridden by volumes in docker-compose)
COPY . .

# Expose port 3000
EXPOSE 3000

# Start development server
CMD ["npm", "start"] 